<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <import type="com.spartancookie.hermeslogger.debugToaster.LogType"/>
        <import type="com.spartancookie.hermeslogger.R"/>
        <import type="com.spartancookie.hermeslogger.utils.UtilsKt"/>
        <variable
            name="logDataHolder"
            type="com.spartancookie.hermeslogger.models.LogDataHolder" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/custom_toast_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/toast_background"
        android:padding="10dp"
        android:alpha="0.85"
        android:layout_gravity="center_horizontal">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:src="@drawable/ic_unknown"
            app:logIcon="@{ logDataHolder.type }"
            app:applyTint="@{logDataHolder.type == LogType.Debug ? R.color.white : UtilsKt.NO_RES }"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="5dp"
            android:layout_marginEnd="5dp"
            android:ellipsize="end"
            android:maxEms="15"
            android:maxLines="1"
            android:text="@{ logDataHolder.message }"
            android:textColor="#FFF"
            android:visibility="@{ logDataHolder.message.isEmpty() ? View.GONE : View.VISIBLE }"
            app:layout_constraintBottom_toBottomOf="@+id/icon"
            app:layout_constraintEnd_toStartOf="@id/copy"
            app:layout_constraintStart_toEndOf="@id/icon"
            app:layout_constraintTop_toTopOf="@+id/icon"
            tools:text="My example test My etestMy example testMy example testestMy example test" />

        <ImageView
            android:id="@+id/copy"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="4dp"
            android:src="@drawable/ic_copy"
            android:visibility="@{ logDataHolder.extraInfo == null ? View.GONE : View.VISIBLE }"
            app:layout_constraintBottom_toBottomOf="@+id/icon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/icon" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>